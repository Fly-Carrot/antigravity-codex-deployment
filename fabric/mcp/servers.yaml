version: 1
servers: []

# Public starter policy:
# - Do not enable third-party or network MCP servers by default.
# - Do not commit machine-local command paths here.
# - Generate a local registry from user choices during installation, or copy
#   docs/mcp-integration-guide.md examples into a private, ignored config.
